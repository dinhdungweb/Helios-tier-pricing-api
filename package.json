{
  "name": "helios-tier-pricing-api",
  "version": "1.0.0",
  "description": "Backend API for Helios tier pricing with line item discounts",
  "main": "api/create-draft-order.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "logs": "vercel logs",
    "env": "vercel env ls",
    "test": "curl -X POST http://localhost:3000/api/create-draft-order -H 'Content-Type: application/json' -d @api/test-request.json"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
